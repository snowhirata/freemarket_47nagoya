var child_category = `<%= j(@grand_child_category_num) %>`
if (child_category >= 1) {
  var html = `<%= j(select :item, :grand_child_category_id, options_from_collection_for_select(@grand_child_category, :id, :name),{:include_blank => '---'})%>
              <%= fa_icon 'chevron-down' %>`
  $('#child-category-select').html(html);
}
if (child_category == '') {
  console.log("hello")
  $('#child-category-select').html('');
}
// if (child_category >= 1 ) {
//   var html = `<%= j(select :item, :grand_child_category_id, options_from_collection_for_select(@grand_child_category, :id, :name),{:include_blank => '---'})%>`
//   $('#child-category-select').html(html);
