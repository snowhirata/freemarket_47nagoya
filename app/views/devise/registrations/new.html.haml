.single-container
  %header.single-header
    %h1
      =link_to image_tag('logo.svg'),root_path
    %nav.progress-bar.signup-bar
      %ol.clearfix
        %li
          会員情報
          .progress-status.active#step1
        %li
          電話番号認証
          .progress-status#step2
        %li
          お届け先住所入力
          .progress-status#step3
        %li
          支払い方法
          .progress-status#step4
        %li
          完了
          .progress-status#step5
  %main
    %section
      %h2 会員情報登録
      =form_for(resource, as: resource_name, url: registration_path(resource_name), html: {class: 'l-single-inner'}) do |f|
        = render "devise/shared/error_messages", resource: resource
        .l-single-content#first-form
          .form-group
            = f.label :ニックネーム
            %span.form-require
              必須
            = f.text_field :nickname, autofocus: true, placeholder:'例)メルカリ太郎'
          .form-group
            = f.label :メールアドレス
            %span.form-require
              必須
            = f.email_field :email, placeholder:'例)pc-携帯どちらでも可'
          .form-group
            = f.label :パスワード
            %span.form-require
              必須
            = f.password_field :password, placeholder:'例)6文字以上'
          .form-group
            = f.label :パスワード確認
            %span.form-require
              必須
            = f.password_field :password_confirmation, placeholder:'例)6文字以上'

          .form-group
            %h3.l-chapter-sub-head 本人確認
            %p.l-single-text
              安心・安全にご利用いただくために、お客さまの本人情報の登録にご協力ください。他のお客さまに公開されることはありません。

          .form-group
            = f.label :性
            %span.form-require
              必須
            = f.text_field :last_name, autofocus: true, placeholder: '例)山田'
          .form-group
            = f.label :名
            %span.form-require
              必須
            = f.text_field :first_name, placeholder: '例)彩'
          .form-group
            = f.label :姓カナ
            %span.form-require
              必須
            = f.text_field :last_name_kana, placeholder: '例)ヤマダ'
          .form-group
            = f.label :名カナ
            %span.form-require
              必須
            = f.text_field :first_name_kana, placeholder: '例)アヤ'

          .form-group
            =f.label :生年月日
            %span.form-require
              必須
            <br>
            = f.date_select :birth_year, start_year: 1930, end_year: (Time.now.year - 18), default: Date.new(2000, 1, 1)


          %p.form-info-text
            ※ 本人情報は正しく入力してください。会員登録後、修正するにはお時間を頂く場合があります。

          .form-group
            %p.l-single-text.text-center
              「次へ進む」のボタンを押すことにより、
              =link_to'利用規約',''
              に同意したものとみなします

          .form-group
            = f.button "次に進む",type: :button,class: 'btn-red',id: 'ivent-button1'

          .form-group.text-right
            %p
              =link_to'本人情報の登録について','#'
              =fa_icon "chevron-right"

        .l-single-content#second-form
          .form-group
            = f.label :郵便番号
            %span.form-require
              必須
            = f.text_field :postal_code, placeholder:'例)123-4567'
          .form-group
            =f.label :都道府県
            %span.form-arbitrary
              任意
            .select-wrap
              =f.collection_select :prefecture_id, Prefecture.all, :id, :name, {},class: 'select-default'
              =fa_icon "chevron-down"
          .form-group
            = f.label :市区町村
            %span.form-require
              必須
            = f.text_field :city, placeholder:'例)横浜市緑区'
          .form-group
            = f.label :番地
            %span.form-require
              必須
            = f.text_field :block, placeholder: '例)青山1-1-1'
          .form-group
            = f.label :建物名
            %span.form-require
              必須
            = f.text_field :building, placeholder: '例)柳ビル103'
          .form-group
            = f.label :電話番号
            %span.form-require
              必須
            = f.text_field :phone_number, placeholder: '例)09012345678'
          .form-group
            = f.button "次へ進む",type: :button,class: 'btn-red',id: 'ivent-button2'
        .l-single-content#third-form
          .form-group
            =f.label :カード番号
            %span.form-arbitrary
              必須
            =f.text_field :card_number, class: 'input-default', placeholder: '例)1234567'
            %ul.signup-card-list
              %li
                =image_tag('visa.svg', width:49, height:20)
              %li
                =image_tag('master-card.svg', width:34, height:20)
              %li
                =image_tag('saison-card.svg', width:30, height:20)
              %li
                =image_tag('jcb.svg', width:32, height:20)
              %li
                =image_tag('dinersclub.svg', width:21, height:20)
              %li
                =image_tag('discover.svg', width:32, height:20)
              %li
                =image_tag('american_express.svg', width:32, height:20)
          .form-group
            =f.label :有効期限
            %span.form-arbitrary
              必須
            .select-wrap
              =f.select :exp_month,User.exp_months.keys,{},class: "select-default"
              =fa_icon "chevron-down"
            .select-wrap
              =f.select :exp_year, User.exp_years.keys,{},class: "select-default"
              =fa_icon "chevron-down"
          .form-group
            =f.label :セキュリティコード
            %span.form-arbitrary
              必須
            =f.text_field :security_code,class: 'input-default', placeholder: '例)カード背面4桁or3桁の番号'
          .text-right
            %span.form-question
              ?
            =link_to '#' do
              カード裏面の番号とは？
          .form-group
            =f.submit :登録する,class: 'submit_button'
        
        %section#forth-form
          %h2 会員情報登録完了
          .l-single-content
            .form-group
              .text-center
                ありがとうございます。<br>
                会員登録が完了しました。
            .form-group
              = f.button "メルカリをはじめる！",type: :button,class: 'btn-red',id: 'ivent-button3'

=javascript_include_tag 'registrations_new.js'